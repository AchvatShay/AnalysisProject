% we do not really need this function as it only one line, so I am skipping
% it. Keeping it for the moment but would probably delete it soon

function analysis(name, outputPath, generalProperty, imagingData, BehaveData, eventsList)
feval(name,outputPath, generalProperty, imagingData, BehaveData, eventsList) 
